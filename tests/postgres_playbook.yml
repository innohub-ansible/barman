- hosts: db_master_servers
  vars_files:
    - ../defaults/main.yml
  handlers:
    - include: ../handlers/main.yml
  tasks:
    - include: ../tasks/postgres_ssh.yml
  roles:
    - ../roles/ANXS.postgresql
